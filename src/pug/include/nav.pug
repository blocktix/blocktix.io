nav
  ul
    each item in menu_items
      if item.name == 'signup'
        li.background
          a(href = `#${item.name}`)= item.title
      else
        li.navigation_link
          a(href = `#${item.name}`)= item.title
